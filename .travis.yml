language: java
jdk:
  - openjdk11

branches:
  only:
    - master

# Travis CI 서버의 HOME
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script:
  - "./gradlew clean build"

# CI 실행완료 시 email로 알람

notifications:
  email:
    recipients:
      - denhenker@naver.com
